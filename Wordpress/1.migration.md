1. Zip all files from public_html recomendation is tar because zip can cause errors sometimes
```bash
tar -zcvf [tar_file_name.tar.gz] * .[a-zA-Z0-9]*

# remove brackets where is tar_file_name
```
2. login ssh and dump mysql ( credentials are found in wp-config.php )
3. Create new database on new hosting
4. Extract tar.gz files on new hosting and change config to match new db credentials.
```bash
#extract files
tar -xvzf filename.tar.gz
```
2.  import mysql. [[Databaze|Mysql]] `commands`
3. run querry if you migrating from one site to another
```mysql
#example
UPDATE wp_options 
SET option_value = 'https://powderblue-quail-808777.hostingersite.com'
WHERE option_name IN ('siteurl', 'home');

```

7. login into website and save permalinks few times. This should regenerate cookes etc.

If this proccess does not work run All in one wp migration.